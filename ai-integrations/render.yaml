# Render.com deployment configuration for AI Integrations Service
# This file tells Render how to build and run your AI integrations service

# Build command (runs after pip install)
# pip install -r requirements.txt && python train_molecular_models.py

# Start command (runs after build)
# uvicorn app.main:app --host 0.0.0.0 --port $PORT

# Environment variables needed in Render dashboard:
# PYTHON_VERSION=3.11
# NODE_ENV=production
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_ANON_KEY=your-anon-key
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# FRONTEND_URL=https://your-vercel-app.vercel.app
# BACKEND_URL=https://your-node-api.onrender.com
# OPENROUTER_API_KEY=your-openrouter-key
